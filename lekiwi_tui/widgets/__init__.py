"""widgets — lekiwi-specific reusable widgets (pickers) layered on the framework.

(The framework's generic input widgets — TextField / NumberField — live in
lekiwi_tui.framework.widgets; this package holds lekiwi-aware pickers that depend on
policies/datasets discovery.)
"""
